# Python
__pycache__/
*.py[cod]

# Virtual environment
.venv/

# Secrets / local config - NEVER commit
.env
config.json

# Local auth cache (identity pin, not a secret - still local-only)
.auth_record.json
